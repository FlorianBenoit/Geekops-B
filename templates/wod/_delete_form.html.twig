<form method="post" action="{{ path('app_wod_delete', {'id': wod.id}) }}" onsubmit="return confirm('Are you sure you want to delete this wod?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wod.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
