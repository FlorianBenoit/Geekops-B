{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
     <div class="container">
        <h1 class="mt-5 mb-4">Type</h1>

        <table class="table table-bordered">
        <tbody>
            <tr>
                <th scope="row">Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th scope="row">Name</th>
                <td>{{ type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_type_edit', {'id': type.id}) }}" class="btn btn-success">edit</a>

    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
